

import csv 

with open(''C:/Users/acer_PC/Desktop/python_crawler/try,'a',newline='',encoding='utf-8-sig')as f 

#這裡在開啟 csv 檔案時加上了 newline='' 參數，這是為了讓資料中包含的換行字元可以正確被解析，所以建議在讀取 csv 檔案時都固定加入這個參數。

w=csv.writer(f)  #建立一個寫入CSV黨的物件 使用csv.writer這個function 

